// base du Jquery a inclure toujours en debut (comme le html), $(document).ready(function() est la version longue , utliser version courte comme ci dessous.
$(function(){
// Cibler id text pour modifier l element par hide
$('#firstText').empty();
$('#secondText').remove();
});
// base du Jquery a inclure toujours en debut (comme le html), $(document).ready(function() est la version longue , utliser version courte comme ci dessous.
$(function(){
// Cibler id text pour modifier l element par hide
$('#SecondText').canceled();
});
